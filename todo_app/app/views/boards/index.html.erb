<div class="page">
  <%= render "shared/topbar", active: :boards %>

  <% if @boards.blank? %>
    <div class="empty-state">
      <p>Você ainda não tem quadros. Crie seu primeiro quadro para começar!</p>
    </div>
  <% else %>
    <div class="boards-grid">
      <% @boards.each do |board| %>
        <div class="board-card">
          <div class="board-card-top">
            <div>
              <h3><%= board.title %></h3>
              <p class="muted">
                Criado em <%= board.created_at.strftime("%d/%m/%Y") %>
              </p>
            </div>

            <div class="muted board-tasks">
              <%= image_tag "icons/lists-icon.svg", class: "icon", alt: "" %>
              <span>
                <%= board.lists.count %> listas
              </span>
            </div>
          </div>

          <div class="board-card-actions">
            <%= link_to "Abrir", board_path(board), class: "btn btn-purple btn-wide" %>

            <%= link_to edit_board_path(board), class: "icon-btn", title: "Editar" do %><%= image_tag "icons/edit.svg", class: "icon", alt: "Editar" %><% end %>

            <%= button_to board_path(board), method: :delete, class: "icon-btn icon-btn-danger", title: "Excluir" do %><%= image_tag "icons/trash.svg", class: "icon", alt: "Excluir" %><% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
